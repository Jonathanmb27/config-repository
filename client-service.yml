server:
  port: 8080

spring:
  data:
    mongodb:
      database: clients
      port: 27017
      host: localhost
eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka/
logging:
   conifg: logback.xml
